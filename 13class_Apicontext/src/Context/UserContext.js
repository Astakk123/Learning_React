
// to use the usecontext hook in react to manage the state first we have to 
//create a context see how 

import React from 'react'

const UserContext = React.createContext()

export default UserContext;

// Now it is done we have sussessfuly create context
